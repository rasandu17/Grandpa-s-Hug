[build]
builder = "NIXPACKS"
buildCommand = "true"

[deploy]
startCommand = "/opt/venv/bin/uvicorn main:app --app-dir Backend --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
